# Application
NODE_ENV=development
PORT=3002

# Database Configuration (Cloud - Neon Tech, Supabase, etc)
# Use DB_LINK for cloud databases (automatically enables SSL)
# Example: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech:5432/dbname?sslmode=require
DB_LINK=

# Database Configuration (Local - Docker/PostgreSQL)
# Leave DB_LINK empty to use these values for local database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=challenge_db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRY=1h
